fastapi
uvicorn
pydantic
python-multipart
androguard
httpx
pytest
aiofiles
stix2
# PINNED. Two hard constraints collide here:
#   * Frida 17 REMOVED the built-in `Java` global (the Android bridge became an
#     external module), so classic `Java.perform` scripts must be bundled with
#     frida-java-bridge via frida-compile.
#   * Frida 16 CANNOT LINK on a 16 KB page-size device (this project's
#     google_apis_ps16k AVD): "empty/missing DT_HASH/DT_GNU_HASH".
# 16 KB support landed in 17.x, so 17 is mandatory here and the hook script is
# bundled at build time (see frida_hooks/). The frida-server pushed to the
# device MUST match this version exactly.
frida==17.16.4
frida-tools==14.10.4
# ── Persistent device channel ─────────────────────────────────────────────────
# uiautomator2 runs a UI server ON the device and holds one connection for the
# whole session. Without it every device interaction is a `subprocess.run([adb,
# ...])`: a process spawn plus a TCP connection to the ADB server (which under
# Docker lives on the host). One perception cycle costs six of those and one
# explorer action ten to fifteen.
#
# Measured before this dependency existed: 5 actions in 611 seconds against a
# 300-second budget - about two minutes per click - with coverage_percent 0 and
# 1 of 2 forms completed. The explorer was not choosing badly; it never got to
# choose more than five times.
#
# It is also a correctness dependency, not only a speed one. `input text` types
# into whatever holds focus, and a real run recorded generated credentials typed
# into com.google.android.apps.nexuslauncher because the app had moved to the
# background. uiautomator2's set_text targets the node.
#
# Optional at runtime: sandbox/device_channel.py degrades to the ADB path when
# this is absent, so a missing wheel makes runs slow, never broken.
uiautomator2==3.7.0

# Persistence layer
aiosqlite
databases
# Auth
python-jose[cryptography]
passlib[bcrypt]
bcrypt==3.2.2
# Async extras
anyio

# AI Explorer
google-genai
python-dotenv

# Enterprise Analysis Depdendencies
pytesseract
Pillow
yara-python
reportlab

# Streaming support for Investigation Assistant
sse-starlette
slowapi>=0.1.9
beautifulsoup4

# VIDE fuzzy string matching (engines/vide/fuzzy.py). Soft dependency - a pure
# Python fallback implements the same algorithm - but it is ~100x slower, and
# the string axis runs over every baseline on every analysis.
rapidfuzz>=3.6
SQLAlchemy[asyncio]>=2.0
asyncpg>=0.29.0
