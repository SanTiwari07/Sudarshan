import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sudarshan.baseline.icici',
  appName: 'iMobile Pay',
  webDir: 'dist'
};

export default config;
