import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.kotak811mobilebankingapp.instantsavingsupiscanandpayrecharge',
  appName: 'Kotak Bank: 811 Mobile App',
  webDir: 'dist'
};

export default config;
