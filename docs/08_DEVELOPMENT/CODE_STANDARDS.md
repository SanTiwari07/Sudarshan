# SUDARSHAN — Code Standards & Linting

> **Classification:** OPERATIONAL  

---

- **Python:** PEP 8 compliance, explicit type annotations (`typing`), Google-style docstrings.
- **TypeScript:** Strict type-checking enabled (`tsconfig.json`), no untyped `any` in API boundaries.
- **Security:** Zero hardcoded credentials. All secrets injected via environment variables.
